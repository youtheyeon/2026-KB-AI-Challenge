# 이슈 1 구현 체크리스트

- [x] `feat/28` 브랜치를 생성한다.
- [x] 구현 계획과 컨텍스트 노트를 작성한다.
- [x] 기존 백엔드 테스트의 기준 상태를 확인한다.
- [x] 사업자 등록 API 실패 테스트를 작성하고 예상한 이유로 실패하는지 확인한다.
- [x] 데이터베이스 세션 의존성을 구현한다.
- [x] 익명 데모 세션 쿠키 생성·조회·만료 처리를 구현한다.
- [x] 사업자 등록 요청·응답 스키마를 구현한다.
- [x] 사업자 등록 서비스와 `POST /api/businesses` 라우터를 구현한다.
- [x] API 라우터를 FastAPI 애플리케이션에 등록한다.
- [x] 관련 테스트와 전체 백엔드 검사를 실행한다.
- [x] OpenAPI 요청·응답 계약과 Git diff를 최종 검토한다.
- [x] 직접 최종 검토 결과를 반영하고 기록 파일을 커밋한다.

# 이슈 2 구현 체크리스트

- [x] 최신 `develop`에서 `feat/29` 브랜치를 생성한다.
- [x] 이슈 #29와 기존 데이터 모델을 확인한다.
- [x] 구현 계획과 컨텍스트 노트를 작성한다.
- [x] `python-multipart`, `openpyxl` 의존성을 구성한다.
- [x] 업로드와 상태 조회 실패 테스트를 작성하고 RED를 확인한다.
- [x] 사업자와 현재 데모 세션의 소유 관계를 검증한다.
- [x] 매출·비용 필수, 온라인 매출 선택의 multipart 업로드 API를 구현한다.
- [x] xlsx 헤더 탐지와 자동 컬럼 매핑을 구현한다.
- [x] 누락 컬럼과 매핑 신뢰도를 파일 메타데이터에 저장한다.
- [x] 업로드 데이터를 표준 매출·비용·온라인 매출 행으로 정규화한다.
- [x] 데이터셋 처리 상태 전이와 상태 조회 API를 구현한다.
- [x] PostgreSQL 성공·롤백 통합 테스트를 작성한다.
- [x] OpenAPI 계약과 전체 테스트·Ruff 검사를 확인한다.
- [x] 구현 변경과 작업 기록을 의미 단위로 커밋한다.

# 이슈 3 구현 체크리스트

- [x] Notion의 사업 상태 분석 및 병목 진단 API 계약을 확인한다.
- [x] BackgroundTasks와 캐시된 공공데이터 사용 설계를 확정한다.
- [x] 설계 문서를 작성하고 검토한다.
- [x] 격리 워크트리와 기준 테스트 상태를 확인한다.
- [x] 구현 계획과 컨텍스트 노트를 작성한다.
- [x] 고정 벤치마크와 순수 지표·병목 계산을 TDD로 구현한다.
- [x] 사업·공공데이터 스냅샷과 백그라운드 상태 전이를 구현한다.
- [x] 진단 실행·조회 API와 오류 계약을 구현한다.
- [x] PostgreSQL 영속화 통합 테스트를 추가한다.
- [x] 전체 테스트, Ruff, OpenAPI 계약, Git diff를 검증한다.
- [x] 구현 변경과 작업 기록을 의미 단위로 커밋한다.

## 이슈 3 코드 리뷰 후속 체크리스트

- [x] 파괴적 PostgreSQL 테스트에 명시적 동의와 `_test` DB명 검증을 추가한다.
- [x] 빈 READY 데이터셋도 먼저 `RUNNING`을 저장하고 백그라운드 실패를 허용한다.
- [x] 일부 행만 정산된 온라인 데이터의 정산율 병목을 생략한다.
- [x] 만료 세션의 진단 실행·조회를 차단한다.
- [x] 데이터셋·진단 행 잠금으로 동시 요청과 중복 작업을 직렬화한다.
- [x] 낮은 비중의 양수 시간대 벤치마크도 비교한다.
- [x] `FAILED` 응답과 PostgreSQL 실패 원자성 테스트를 추가한다.
- [x] 리뷰 수정 후 전체 검증과 재검토를 통과한다.
- [x] 리뷰 수정 내용을 의미 단위로 커밋한다.

# 이슈 34 구현 체크리스트

- [x] 현재 작업 트리를 보존하고 `feat/34` 브랜치를 생성한다.
- [x] GitHub 이슈 #34와 기존 시뮬레이션·AI 구조를 확인한다.
- [x] 동기 생성, 식별자 응답, 서비스·AI 어댑터 분리 방식을 확정한다.
- [x] 저장된 완료 진단을 사용하는 데이터 흐름과 오류·테스트 정책을 확정한다.
- [x] 설계 문서를 작성하고 자체 검토한 뒤 사용자 승인을 받는다.
- [x] 상세 구현 계획을 작성한다.
- [x] 기준 테스트를 실행하고 현재 상태를 기록한다.
- [x] 실패 테스트를 먼저 작성하고 예상한 이유로 실패하는지 확인한다.
- [x] 저장 진단 기반 AI 시나리오 생성과 상환 계산을 구현한다.
- [x] 같은 프로세스 AI 엔진 어댑터와 안전한 예외 경계를 구현한다.
- [x] 동기 생성 서비스의 소유권·선행 상태 검증과 원자적 결과 저장을 구현한다.
- [x] 저장 결과·중립 비교 조회와 잠금 전 시나리오 선택 변경을 구현한다.
- [x] 시뮬레이션 서비스와 네 API를 구현한다.
- [x] API·서비스·PostgreSQL 통합 테스트를 통과시킨다.
- [x] OpenAPI 계약, Ruff와 전체 백엔드 테스트를 최종 확인한다.
- [x] 구현과 작업 기록을 의미 단위로 커밋한다.

# 이슈 36 구현 체크리스트

- [x] 최신 `develop`에서 `feat/36` 브랜치를 생성하고 사용자 작업 트리를 보존한다.
- [x] Notion의 결과 검증 시뮬레이션 여섯 API와 GitHub 이슈 #36을 확인한다.
- [x] 실행 항목, 사후 데이터 입력, 90일 기준과 불변 생성 정책을 확정한다.
- [x] 기존 `ai/` 재사용 경계, 수동 입력 한계와 대시보드 상환 추정 정책을 확정한다.
- [x] 오류·트랜잭션·동시성·테스트 전략을 확정한다.
- [x] 설계 문서를 작성하고 자체 검토한다.
- [ ] 설계 문서를 사용자에게 검토받는다.
- [ ] 상세 구현 계획을 작성한다.
- [ ] 기준 테스트 상태를 확인한다.
- [ ] 도메인·마이그레이션 실패 테스트를 작성하고 RED를 확인한다.
- [ ] 실제 집행과 검증 대상 조회를 TDD로 구현한다.
- [ ] 세 가지 사후 데이터 입력을 TDD로 구현한다.
- [ ] `OutcomeEngine`과 결과 검증 저장·조회를 TDD로 구현한다.
- [ ] 선순환 대시보드를 TDD로 구현한다.
- [ ] API·서비스·PostgreSQL 통합 테스트를 통과시킨다.
- [ ] OpenAPI 계약, 전체 pytest, Ruff와 Git diff를 최종 검증한다.
- [ ] 구현과 작업 기록을 의미 단위로 커밋한다.
