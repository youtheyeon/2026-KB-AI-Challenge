# 이슈 1 컨텍스트 노트

## 2026-07-31

- 이슈 1만 `feat/28` 브랜치에서 구현하며 이슈 2는 후속 작업으로 남긴다.
- 현재 작업 트리의 `.gitignore` 수정과 `docs/project-proposal-plan.md`는 사용자 변경이므로 건드리지 않는다.
- 기존 `Business`는 필수 `demo_session_id` 외래키를 가지므로 사업자 등록 API가 데모 세션을 먼저 해석하거나 생성해야 한다.
- 쿠키가 없거나 세션이 만료되었으면 새 `DemoSession`을 생성하고, 유효한 쿠키가 있으면 기존 세션을 재사용한다.
- 외부 API 필드는 camelCase를 사용하고 SQLAlchemy 모델 필드는 기존 snake_case를 유지한다.
- 이메일 로그인, Supabase Auth, 데이터 업로드와 컬럼 매핑은 이번 범위에 포함하지 않는다.
- 새 동작은 실패 테스트를 먼저 작성하고 RED를 확인한 뒤 최소 구현한다.
- 시스템에 `uv` 실행 파일이 없지만 `backend/.venv`에 pytest와 Ruff가 준비되어 있다.
- 기준 검증은 `.venv/bin/pytest -q`에서 33개 통과, 3개 스킵이며 Ruff 검사도 통과했다.
- 데모 세션 쿠키 이름은 `demo_session_id`, 신규 세션 유효 기간은 24시간으로 정했다.
- `BACKEND_ENVIRONMENT=production`에서만 쿠키의 `Secure` 속성을 활성화한다.
- `employeeCount`와 `primarySalesChannels`는 각각 `0`과 빈 배열을 기본값으로 사용한다.
- 저장소 문자열·정수 한계를 요청 스키마에 반영하고 공백 정규화를 길이 검증 전에 수행한다.
- 미존재 UUID와 비활성 세션 분기를 API 테스트로 보호한다.
- 실제 PostgreSQL 성공·롤백 통합 테스트를 추가했지만 현재 `TEST_DATABASE_URL`이 없어 실행은 스킵됐다.
- 작업 단위 리뷰에서 `aa3688c..3a44bcb` 범위의 Critical과 Important 항목이 모두 해소됐다.
- 사용자의 요청에 따라 추가 서브 에이전트 검토 없이 최종 검증을 직접 수행했다.
- 최종 검증에서 `.venv/bin/pytest -q`는 51개 통과와 5개 스킵, Ruff 검사와 포맷 검사는 통과했다.
- OpenAPI에서 `POST /api/businesses`의 `201` 응답과 camelCase 요청 필드를 확인했다.

## 이슈 2 — 2026-07-31

- 이슈 #29만 `feat/29` 브랜치에서 구현하고 `PATCH /api/datasets/{datasetId}/mapping`은 제외한다.
- 작업 트리의 `.gitignore` 수정과 `docs/project-proposal-plan.md`는 사용자 변경이므로 건드리지 않는다.
- Notion 원본 페이지는 연결 권한에서 `NOT_FOUND`였으며 GitHub 이슈 #29의 계약을 기준으로 구현한다.
- 매출과 비용 xlsx는 필수이고 온라인 매출 xlsx는 선택으로 처리한다.
- 원본 파일은 영구 저장하지 않고 `DatasetFile.storage_path`를 비워 둔다.
- 자동 컬럼 매핑, 누락 컬럼, 신뢰도, 행 수는 기존 `DatasetFile.file_metadata` JSONB에 저장한다.
- 새 테이블이나 컬럼 없이 기존 `Dataset`, `DatasetFile`, 정규화 모델을 사용한다.
- 업로드 요청 안에서 파싱과 정규화를 수행하고 접수 응답은 `202`, 상태 조회는 최종 처리 상태를 반환한다.
- 매출은 영업일자와 순매출, 비용은 거래일자·비용항목·합계금액, 온라인 매출은 영업일자와 매출금액을 필수 매핑으로 본다.
- 누락 필수 컬럼은 `needs_reupload`, 손상된 xlsx나 행 변환 실패는 `failed`로 저장한다.
- 상권 파일 파트는 받지 않으며 외부 공공데이터 클라이언트가 없는 현재 범위에서는 사업자의 지역 정보를 훼손하지 않는다.
- `python-multipart 0.0.32`, `openpyxl 3.1.5`를 추가하고 `uv.lock`을 갱신했다.
- 지원 헤더가 일부 또는 전부 누락된 정상 xlsx는 `needs_reupload`, 압축 구조가 손상된 파일은 `failed`로 구분한다.
- 빠른 API 테스트에서 필수·선택 파일, 정규화, 메타데이터, 실패 상태, 세션 격리를 검증한다.
- PostgreSQL 통합 테스트는 JSONB 메타데이터와 세 정규화 테이블 저장, 파일 insert 실패 시 롤백을 검증하도록 작성했다.
- 현재 `TEST_DATABASE_URL`과 Docker 실행 파일이 없고 로컬 55432 포트 연결도 거부돼 PostgreSQL 테스트 7건은 스킵된다.
- 최종 전 검증에서 전체 테스트는 61개 통과와 7개 스킵이며 Ruff 검사와 multipart OpenAPI 계약 확인은 통과했다.
- 구현 변경은 `30657c4` 커밋으로 분리했다.

## 이슈 3 — 2026-07-31

- `POST /api/businesses/{businessId}/diagnoses`와 `GET /api/diagnoses/{diagnosisId}`를 구현한다.
- 실행 API는 FastAPI `BackgroundTasks`로 `202/RUNNING`을 반환하고 요청 세션과 분리된 DB 세션에서 `COMPLETED` 또는 `FAILED`로 전이한다.
- 진단 요청 중 공공 API를 호출하지 않고 `ai/raw_data/seoul_cafe_sales_full.json`의 최신 분기에서 만든 고정 벤치마크를 사용한다.
- 최신 분기는 `20242`, 표본은 326개 상권, 월매출 중앙값은 94,283,406원, 월주문 중앙값은 11,729건이다.
- 공공데이터 캐시에 유동인구가 없으므로 `floatingPopulationGrowthRate`는 추정하지 않고 `null`로 반환한다.
- 월별 지표는 데이터셋의 가장 최신 매출 연월을 기준으로 매출·비용·온라인 자료를 동일 기간에서 집계한다.
- 온라인 자료가 없으면 온라인 매출 비중은 `null`이고 온라인 관련 병목을 만들지 않는다.
- 기존 `BusinessSnapshot`, `PublicDataSnapshot`, `Diagnosis`, `DiagnosisMetric`, `Bottleneck`을 사용하며 스키마 마이그레이션은 추가하지 않는다.
- 요청 검증 실패는 진단 실행 경로에서만 `400`으로 변환하고 기존 API의 `422` 계약은 유지한다.
- 현재 `develop`의 `.gitignore` 수정과 `docs/project-proposal-plan.md`는 사용자 변경이므로 `/tmp/kb-ai-business-diagnosis-api` 격리 워크트리에서 작업한다.
- 구현 전 기준 검증은 전체 테스트 61개 통과·7개 스킵, Ruff 검사 통과다.
- 새 동작은 실패 테스트를 먼저 실행해 RED를 확인한 뒤 최소 구현한다.
- 순수 분석 서비스의 RED는 모듈 부재로 확인했고 구현 후 테스트 6개와 대상 Ruff 검사가 통과했다.
- 벤치마크는 최신 분기 326건의 단순 중앙값을 Python `statistics.median`으로 계산해 94,283,406원으로 고정했다.
- 최신 월 집계와 백그라운드 상태 전이의 RED를 확인했고 구현 후 서비스 테스트 총 10개와 대상 Ruff 검사가 통과했다.
- 백그라운드 작업은 요청 세션을 재사용하지 않으며 실패 시 계산 트랜잭션을 롤백하고 진단 상태만 `FAILED`로 갱신한다.
- 진단 라우터의 RED는 모듈 부재로 확인했고 구현 후 API 테스트 7개와 대상 Ruff 검사가 통과했다.
- 진단 요청의 400 오류 변환은 전용 `APIRoute`에만 적용해 기존 API의 FastAPI 422 계약은 유지한다.
- PostgreSQL 통합 테스트는 실제 스냅샷·지표·병목 영속화를 검증하며 `TEST_DATABASE_URL`이 없는 현재 환경에서는 1개가 안전하게 스킵됐다.
- 운영 Supabase 연결은 테스트에 사용하지 않았고 스키마나 데이터를 변경하지 않았다.
- `BackgroundTasks`는 프로세스 재시작 시 실행 중인 작업을 복구하지 못하므로 단일 EC2용 초기 구현으로 문서화했다.
- 단위·통합 테스트의 같은 파일명이 pytest 수집 충돌을 일으켜 통합 테스트를 `test_diagnosis_persistence.py`로 변경했고 대상 테스트 7개 통과·1개 스킵으로 회귀를 확인했다.
- 최종 검증은 전체 테스트 78개 통과·8개 스킵, Ruff 검사·포맷 검사·OpenAPI 진단 계약·`git diff --check` 통과다.
- 독립 코드 리뷰에서 파괴적 테스트 DB 오지정, 빈 매출의 동기 실패, 일부 정산 집계 왜곡, 만료 세션 재사용, 스냅샷·작업 동시성 문제가 병합 차단 항목으로 확인됐다.
- 통합 테스트는 `ALLOW_DESTRUCTIVE_TEST_DATABASE_RESET=1`과 `_test`로 끝나는 DB명을 모두 요구하며, 더미 `postgres` DB명으로 실행했을 때 연결 전에 차단되는 것을 확인했다.
- 빈 매출 데이터셋은 0원 입력 스냅샷과 `RUNNING` 진단을 먼저 저장하고 기존의 엄격한 분석 수집기가 백그라운드에서 `FAILED`로 전이시킨다.
- 정산액이 누락된 온라인 행이 하나라도 있으면 정산율 관련 입력을 `null`로 두어 모집단이 다른 비율을 계산하지 않는다.
- 진단 소유권은 쿠키 UUID뿐 아니라 세션의 `ACTIVE` 상태와 만료 시각까지 검증한다.
- POST는 데이터셋 행을, 백그라운드 작업은 진단 행을 잠그며 이미 종료된 진단은 재계산하거나 실패 상태로 덮어쓰지 않는다.
- 리뷰 수정 후 전체 테스트 89개 통과·9개 스킵, Ruff 검사·포맷 검사·OpenAPI 계약·`git diff --check`가 통과했다.
- 독립 재검토에서 남은 Critical·Important 항목과 새 병합 차단 항목이 없음을 확인했다.

## 이슈 34 — 2026-07-31

- 현재 `develop`의 `f3e9b8a`에서 `feat/34` 브랜치를 생성했다.
- 작업 트리의 `.gitignore` 수정과 `docs/project-proposal-plan.md`는 사용자 변경이므로 건드리지 않는다.
- 시뮬레이션 생성은 동기 처리하고 `201` 응답에는 `simulationId`와 완료 상태만 반환한다.
- 상세 결과는 `GET /api/simulations/{simulationId}`에서 조회한다.
- 라우터, 시뮬레이션 서비스, 같은 프로세스의 AI 어댑터를 분리한다.
- 저장된 `COMPLETED` 진단을 배분 근거의 단일 원천으로 사용하고 AI 엔진에서 병목을 다시 진단하지 않는다.
- AI 엔진의 시나리오 생성 단계를 별도 함수로 추출해 기존 `run_simulation()`과 백엔드 어댑터가 함께 사용한다.
- LLM 호출 중 데이터베이스 트랜잭션을 열어 두지 않고, AI 생성 성공 후 짧은 저장 트랜잭션으로 전체 결과를 저장한다.
- 생성 실패 시 시뮬레이션 행을 남기지 않으며 자동 테스트에서 실제 LLM이나 외부 API를 호출하지 않는다.
- 생성 요청은 세 상환 방식과 거치 기간을 저장하고 대표 월 상환액 계산에도 반영한다.
- 다른 세션 소유 자원은 `404`, 선행 상태 충돌과 선택 잠금은 `409`, AI 생성 실패는 `502`로 처리한다.
- 사용자가 `2026-07-31-fund-allocation-simulation-api-design.md` 설계를 승인했다.
- 구현은 AI 계산 코어, 같은 프로세스 어댑터, 생성 서비스, 조회·선택 서비스, HTTP 계약, PostgreSQL 검증 순서로 진행한다.
- 백엔드 CI는 `backend/`만 Ruff 검사하며 기존 `ai/` 파일에는 작업 전부터 21개 위반이 있으므로, 무관한 형식 수정을 피하고 AI 변경은 전용 단위 테스트로 검증한다.
- 구현 시작 기준 검증에서 백엔드 전체 테스트는 61개 통과와 7개 스킵이며, Ruff 검사와 포맷 검사는 통과했다.
- AI 계산 코어는 저장된 진단을 직접 받아 A·B·C안을 생성하며 원리금균등, 원금균등, 만기일시 상환과 거치 기간을 대표 월 상환액에 반영한다.
- AI 계산 코어 구현 후 백엔드 전체 테스트는 69개 통과와 7개 스킵이며, Ruff 검사와 포맷 검사는 통과했다.
- 같은 프로세스 어댑터는 AI 모듈을 지연 로딩하고 런타임·HTTP 오류와 잘못된 결과 형상을 비밀 정보가 없는 `SimulationGenerationError`로 변환한다.
- AI 어댑터 구현 후 백엔드 전체 테스트는 72개 통과와 7개 스킵이며, Ruff 검사와 포맷 검사는 통과했다.
- 생성 서비스는 소유권과 진단·데이터셋 상태를 확인한 후 읽기 트랜잭션을 종료하고, AI 결과가 도메인 불변조건을 만족할 때만 전체 시나리오 그래프를 한 트랜잭션으로 저장한다.
- 생성 서비스 구현 후 백엔드 전체 테스트는 78개 통과와 7개 스킵이며, Ruff 검사와 포맷 검사는 통과했다.
- 결과와 비교 조회는 저장된 시나리오를 고정 순서와 대문자 enum으로 투영하며 AI 엔진을 다시 호출하지 않는다.
- 시나리오 선택은 같은 시뮬레이션 소속만 허용하고 집행 잠금 전에는 변경할 수 있으며, 잠금 이후 변경은 `409 SELECTION_LOCKED`로 거부한다.
- 조회·비교·선택 서비스 구현 후 백엔드 전체 테스트는 83개 통과와 7개 스킵이며, Ruff 검사와 포맷 검사는 통과했다.
- 네 HTTP API는 camelCase 필드와 대문자 enum을 사용하고, 서비스 오류와 요청 검증 오류를 입력값·내부 원문이 없는 구조화 응답으로 반환한다.
- OpenAPI에서 네 경로, 생성 `201` 응답과 생성 요청의 여섯 camelCase 속성을 확인했다.
- HTTP 계층 구현 후 백엔드 전체 테스트는 91개 통과와 7개 스킵이며, Ruff 검사와 포맷 검사는 통과했다.
- 로컬 임시 PostgreSQL을 격리 실행해 새 저장·롤백 테스트 2건과 기존 통합 테스트를 포함한 전체 100개 테스트가 통과했다.
- PostgreSQL 테스트 URL은 설치된 `psycopg` 3 드라이버를 사용하도록 `postgresql+psycopg://` 형식으로 실행했다.
- 최종 무환경 검증은 핵심 집중 테스트 30개 통과, 전체 테스트 91개 통과와 PostgreSQL 테스트 9개 스킵, Ruff 검사와 포맷 검사 통과로 확인했다.
- 최종 OpenAPI 검사에서 네 경로, 생성 `201` 응답과 camelCase 생성 요청 속성을 다시 확인했다.
- 자동 테스트는 실제 Anthropic API를 호출하지 않으므로 운영 키·네트워크를 사용한 LLM 호출은 배포 환경의 남은 확인 항목이다.

## 이슈 36 — 2026-07-31

- 최신 `develop`의 `6b1a6c3`에서 `feat/36` 브랜치를 생성했다.
- 작업 트리의 `.gitignore` 수정과 `docs/project-proposal-plan.md`는 사용자 변경이므로 건드리지 않는다.
- 구현 범위는 검증 대상, 실제 집행, 사후 데이터, 결과 검증 생성·조회와 사업 대시보드의 여섯 API다.
- 검증 대상은 완료 시뮬레이션, 최종 선택 존재와 생성 UTC 날짜 기준 90일 이상을 모두 요구하며 정확히 90일부터 포함한다.
- 집행, 사후 데이터와 결과 검증은 시뮬레이션당 하나인 불변 기록으로 만들고 중복 POST는 `409`로 거부한다.
- `ScenarioSelection`은 당시 결정의 감사 기록으로 보존하며 실제 집행이 다른 시나리오나 자유 배분이어도 허용한다.
- 실제 집행 생성과 함께 선택을 잠그고, 자유 배분을 위해 `ExecutionAllocation.name`을 추가하고 `category`를 nullable로 변경한다.
- 사후 데이터 URL 하나에서 MOCK·MANUAL_INPUT JSON과 FILE_UPLOAD multipart를 Content-Type으로 구분한다.
- 모든 사후 입력은 데이터셋과 다음 사업 스냅샷을 생성하며 수동 입력의 네 지표는 `OutcomeData`에 구조화해 저장한다.
- `OutcomeEngine`은 새 계산기가 아니라 `ai/outcome_tracker.py`의 `compare_outcomes()`와 `ai/mock_pos_data.py`의 생성기를 감싸는 백엔드 어댑터다.
- 기존 `compare_outcomes()`가 내부에서 병목 탐지와 재무 계산을 호출하므로 MOCK과 FILE_UPLOAD에서 이 경로를 재사용한다.
- MANUAL_INPUT 네 지표만으로 전체 병목을 재진단하지 않고 지표 비교만 수행하며 병목 재평가는 `NOT_COMPARABLE`로 저장한다.
- 지표 상태와 전체 성과 상태를 별도 enum으로 관리해 `ABOVE_EXPECTED` 계열과 `MET` 계열을 혼용하지 않는다.
- 조회와 대시보드는 저장된 결과만 투영하며 `ai/` 계산을 다시 실행하지 않는다.
- 실제 상환 이력 모델과 입력 API가 없으므로 대시보드는 대출 조건과 경과 개월로 추정하고 `repaymentDataType=ESTIMATED`를 반환한다.
- 파일 파싱과 AI 계산 중에는 DB 트랜잭션을 열어 두지 않고, 저장 직전에 행을 다시 잠가 선행 상태와 중복을 재검증한다.
- 유일 제약과 행 잠금을 함께 사용하고 실패 시 부분 데이터셋이나 비교 그래프를 남기지 않는다.
- 입력 오류는 `400`, 미존재·다른 세션은 `404`, 선행 상태·중복은 `409`, Pydantic 검증은 `422`, AI 계산 실패는 `502`로 구분한다.
- 자동 테스트는 실제 LLM, 외부 API와 네트워크를 호출하지 않는다.
- 자체 검토에서 FILE_UPLOAD의 매출·비용 파일만으로 재구매율·좌석·온라인 정산 병목을 관측할 수 없음을 확인했다.
- `compare_outcomes()`와 `detect_bottlenecks()`에 선택적 비교 가능 유형을 추가해 미관측 병목을 해결이 아닌 `NOT_COMPARABLE`로 유지한다.
- MOCK은 전체 병목, FILE_UPLOAD는 파일에서 산출 가능한 병목만 재평가하고 MANUAL_INPUT은 병목을 재평가하지 않는다.
- 자유 집행 항목은 기존 재무 계산기의 미분류 항목 가정인 12개월간 50% 반복 지출을 적용하고 `DOMAIN_ASSUMPTION`으로 표시한다.
- 지표 상태는 목표값 이상, 손익분기 이상, 손익분기 미만의 세 구간으로 판정하며 비교 근거가 없으면 `NOT_COMPARABLE`로 둔다.
- 수동 입력의 온라인 주문 비중은 의미가 다른 스냅샷의 온라인 매출 비중에 복사하지 않고 `OutcomeData`에만 원본으로 저장한다.
- `includeCompleted=false`는 집행 등록 여부가 아니라 결과 검증 완료 여부를 기준으로 거르며, 집행만 등록된 대상은 후속 입력을 위해 계속 노출한다.
- 성공한 FILE_UPLOAD는 자동 매핑 완료를 뜻하는 `MAPPING_READY`로 저장하고 결과 생성에서 `READY`와 함께 허용한다.
- 손상 파일과 필수 컬럼 누락은 사후 데이터 유일 슬롯을 소비하지 않고 `400`으로 거부해 지원 형식으로 다시 요청할 수 있게 한다.
- 사용자가 `2026-07-31-result-verification-api-design.md` 설계를 승인했다.
- 구현은 도메인·후속 마이그레이션, AI 부분 비교, 같은 프로세스 어댑터, 검증 대상·집행, 사후 데이터, 결과 비교, 대시보드, HTTP 계약, PostgreSQL 통합 검증 순서로 진행한다.
- Mock의 전체 POS 입력은 결과 생성 사이의 재현성을 위해 `OutcomeData.raw_pos_data` JSONB에 저장한다.
- 외부 API enum은 대문자 이름을 사용하고 데이터베이스 enum 값은 기존 코드 스타일에 맞춰 소문자로 저장한다.
- 최신 원격 `develop`의 `bd9b960`에는 `ai/business_history.py`를 중심으로 개인 기준선, 지속 병목 최소 비중 상향과 부작용 이력 추적이 추가돼 있어 `feat/36`에 병합했다.
- 병합 후 AI·시뮬레이션 집중 회귀 테스트는 `.venv/bin/pytest tests/ai tests/services/test_simulation_engine.py tests/services/test_simulation_service.py -q`로 22개 통과했다.
- 결과 비교가 반환하는 `next_round_pos_data_snapshot`을 비교 행과 함께 저장하고 대시보드의 다음 초기 조건 및 향후 개인 기준선 입력으로 보존한다.
- 완료 비교의 `REMAINING`·`NEW` 병목, 실제 집행 카테고리 비율과 저장 POS 스냅샷을 사업장별 `business_history`로 만들어 다음 시뮬레이션 생성에 전달한다.
- 카테고리가 없는 자유 집행 항목은 선순환 이력에서 다른 카테고리로 추측하지 않고 제외하며, 비율을 다시 정규화하지 않는다.
- 백엔드 진단의 대문자 병목 코드와 AI 소문자 코드가 달라 현재 경계에서 그대로는 선순환 규칙이 발동하지 않으므로 명시적 코드 매핑을 추가한다.
- 현재 `SimulationService`는 저장된 완료 진단을 단일 원천으로 사용하므로 과거 POS로 병목을 재진단하는 개인 기준선 치환은 활성화하지 않는다. 지속 병목 상향과 부작용 경고만 연결하고 개인 기준선은 향후 진단 생성 경로에 맡긴다.
- 최신 병합·계획 보완 후 기준 검증은 전체 백엔드 테스트 119개 통과와 11개 스킵, Ruff 검사 통과, 57개 파일 포맷 확인 통과다.
- 도메인 RED는 전용 사후 데이터 enum과 자유 집행 컬럼 부재로 실패했고, 최소 모델 변경 후 도메인 테스트 14개가 통과했다.
- `/tmp/kb-ai-pg-feat36.LV7F5Q`의 일회성 PostgreSQL 55432 포트와 `kb_domain_test` DB에서 `20260731_0003` 업·다운·재업그레이드 테스트 2개가 통과했다.
- AI 결과 추적 RED는 부분 비교·대출 조건·`business_history` 인자 부재로 4개 실패했고, 최소 확장 후 AI 테스트 14개가 통과했다.
- 부분 사후 데이터는 `comparable_bottleneck_types`에 포함된 필드만 읽으며, 관측하지 않은 기존 병목을 `not_comparable_bottlenecks`로 보존한다.
- 저장 진단용 `run_allocation_simulation()`은 선택적 완료 이력에서 지속 병목 최소 비중과 부작용 경고만 계산하고 개인 기준선 재진단은 수행하지 않는다.
- AI 변경 후 전체 백엔드 회귀 테스트는 131개 통과와 11개 스킵이며 새 AI 테스트 Ruff·포맷 검사가 통과했다.
- `OutcomeEngine` RED는 어댑터 모듈 부재로 실패했고, Mock 생성·부분 병목 비교·재무 재계산 지연 로더와 안전한 형상 검증을 구현한 뒤 집중 테스트 5개가 통과했다.
- 어댑터는 원래 AI 예외 메시지를 공개하지 않고 모든 계산 경계 실패를 `OutcomeCalculationError`로 변환한다.
- 검증 서비스 RED는 모듈 부재로 실패했고, 90일 경계·완료 필터·세션 소유권·시나리오 복사·자유 집행·중복·선택 잠금을 구현한 뒤 집중 테스트 11개가 통과했다.
- `includeCompleted=false`는 비교 결과가 있는 시뮬레이션만 제외하며 집행만 등록된 사이클은 `executionRegistered=true`로 유지한다.
- 검증 대상·집행 서비스 이후 전체 백엔드 회귀는 147개 통과와 11개 스킵이며 Ruff 검사와 63개 파일 포맷 검사가 통과했다.
- 사후 데이터 RED는 서비스 모듈 부재로 실패했고, 실제 xlsx 파서와 가짜 저장소를 사용한 MOCK·FILE_UPLOAD·MANUAL_INPUT 집중 테스트 9개가 통과했다.
- 파일 손상·필수 컬럼 누락과 잘못된 수동 지표는 저장 트랜잭션 전에 거부해 데이터셋·스냅샷·사후 데이터 슬롯을 소비하지 않는다.
- 수동 온라인 주문 비중은 `OutcomeData.online_order_ratio`에만 보존하고 의미가 다른 `BusinessSnapshot.online_sales_ratio`는 `None`으로 둔다.
- 사후 데이터 서비스 이후 전체 백엔드 회귀는 156개 통과와 11개 스킵이며 Ruff 검사와 65개 파일 포맷 검사가 통과했다.
- 결과 비교 서비스 RED는 `app.services.outcome` 모듈 부재로 확인했고, 목표·손익분기 구간 판정과 결과 그래프 저장·조회를 구현한 뒤 집중 테스트 14개가 통과했다.
- 백엔드 진단 병목은 `ai_contract.py`의 명시적 매핑을 거쳐 AI 결과 추적 코드로 변환하며, 이미 AI 코드인 값과 알 수 없는 값은 그대로 보존한다.
- 수동 입력은 재무 계산만 수행하고 기존 병목을 `NOT_COMPARABLE`로 저장하며, Mock은 전체 병목, 파일은 원가·인건비와 시간대 자료 완전성에 따라 관측 가능한 병목만 비교한다.
- 결과 비교는 AI가 반환한 다음 POS 스냅샷, 실제 집행 비율과 해결·잔존·신규 병목 변화를 함께 영속화하고 조회 시 AI를 다시 호출하지 않는다.
- 결과 비교 서비스 이후 전체 백엔드 회귀는 올바른 `backend/` 작업 디렉터리에서 165개 통과와 11개 스킵이며 Ruff 검사와 73개 파일 포맷 검사가 통과했다.
- 새 시뮬레이션은 같은 사업장의 완료 비교를 시간순 회차 이력으로 만들고 `REMAINING`·`NEW` 병목, 다음 POS 스냅샷과 실제 카테고리 집행 비율을 AI `business_history` 인자로 전달한다.
- 카테고리가 없는 자유 집행 금액은 선순환 배분 이력에서 제외하고 남은 비율을 재정규화하지 않으며, 백엔드 진단과 과거 변화의 병목 코드는 모두 같은 AI 코드 경계를 통과한다.
- 선순환 연결 후 AI 지속 병목·부작용 테스트를 포함한 집중 테스트 20개와 전체 백엔드 테스트 166개가 통과했고 11개는 환경 조건으로 스킵됐으며 Ruff 검사와 73개 파일 포맷 검사가 통과했다.
- 대시보드 RED는 `app.services.dashboard` 모듈 부재로 확인했고, 완전 경과 개월 기준 원리금균등·원금균등·만기일시·거치·만기 상한의 상환 추정 테스트 8개가 통과했다.
- 대시보드는 실제 납부 이력으로 오인하지 않도록 `repaymentDataType=ESTIMATED`를 고정하며 가장 최근 집행의 대출 조건과 선택 시나리오의 대표 월 상환액을 사용한다.
- 저장된 비교 지표, 완료 사이클, 최신 `REMAINING`·`NEW`·`NOT_COMPARABLE` 병목과 다음 POS 스냅샷만 투영하며 결과 엔진을 주입하거나 재호출하지 않는다.
- 대시보드 서비스 이후 전체 백엔드 테스트 174개가 통과했고 11개는 환경 조건으로 스킵됐으며 Ruff 검사와 75개 파일 포맷 검사가 통과했다.
- 여섯 HTTP API RED는 검증 라우터 모듈 부재로 확인했고, 검증 대상·집행·JSON 및 multipart 사후 데이터·결과 생성·조회·대시보드 계약 테스트 13개가 통과했다.
- 사후 데이터 단일 POST 경로는 Content-Type 접두어로 명시 분기하고 수동 검증 오류를 전역 `422 VALIDATION_ERROR` 계약으로 변환하며, 지원하지 않는 Content-Type은 `400`으로 거부한다.
- OpenAPI에서 여섯 경로와 사후 데이터의 `application/json`·`multipart/form-data` 요청 본문을 확인했다.
- HTTP 계층 구현 후 전체 백엔드 테스트 187개가 통과했고 11개는 환경 조건으로 스킵됐으며 Ruff 검사와 77개 파일 포맷 검사가 통과했다.
- 실제 PostgreSQL 통합 테스트는 첫 수동 입력 사이클과 두 번째 Mock 사이클을 API로 완료한 뒤 세 번째 시뮬레이션의 정렬된 `business_history`까지 검증한다.
- 통합 검증에서 자유 집행은 이력 비율에서 제외되고 같은 시나리오 집행의 네 카테고리 비율은 각각 25%로 보존됐으며, 잔존 원가율과 신규 인건비 병목이 AI 코드로 전달됐다.
- 결과 지표 저장을 의도적으로 실패시켰을 때 비교·지표·재평가·병목 변화가 모두 롤백되고 선행 집행과 사후 데이터만 유지되는 것을 실제 PostgreSQL에서 확인했다.
- `/tmp/kb-ai-pg-feat36.LV7F5Q`의 격리 서버와 `kb_domain_test`에서 새 통합 테스트 2개와 PostgreSQL 포함 전체 백엔드 테스트 200개가 통과했다.
- 최종 OpenAPI 참조 검증에서 JSON 사후 데이터 스키마가 자동 등록되지 않은 컴포넌트를 가리키는 문제를 발견해 MOCK·MANUAL_INPUT 인라인 `oneOf` 계약으로 수정하고 참조 무결성 회귀 테스트를 추가했다.
- 최종 검증은 격리 PostgreSQL 포함 전체 테스트 200개, 전체 Ruff 검사, 78개 파일 포맷 검사, 여섯 경로·결과 POST/GET·두 사후 데이터 Content-Type·JSON 스키마 참조 무결성과 `git diff --check` 통과다.
